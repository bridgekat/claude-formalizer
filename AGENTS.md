Read the specified `.tex` file. Perform the following tasks:

* Step 1. Understand the problem and the proof in the `.tex` file. Write a **detailed, mathematically rigorous proof** `Drafts/DetailedProof.tex` in prose. Keep re-reading and improving `Drafts/DetailedProof.tex` until you think it contains no logical gap and can be easily translated to Lean 4 with Mathlib (use tools to search which results are already proven in Mathlib).
* Step 2. In the Lean 4 project, write a **formal skeleton** (a set of formal lemma statements with `sorry`, including the formal statement for the original problem) for the detailed proof. Split lemmas across multiple files, so that each file can be completed individually and contains approximately the same amount of work. Make sure the whole skeleton compiles.
* Step 3. Spawn sub-agents to read `Drafts/DetailedProof.tex` and fill in the `sorry`s in each file. If any of them proves difficult to complete, identify the cause, think if it requires high-level restructuring, and if so, update `Drafts/DetailedProof.tex` to solve the problem (think carefully - you may spend time planning here), update the skeleton to match, and return to Step 2.
* Step 4. Check that the resulting project compiles and is a valid formal proof for the original problem (no remaining `sorry`s, no axiomatic or definitional cheats - the statement of the original problem, including any definition it depends on, must all be correct). If not, identify the errors and return to Step 2.

When spawning sub-agents that modify Lean files, always check project compilation status first, and always let them work on *a separate copy* of the project. You should update the original files from the modified copy only after the copy compiles (with or without `sorry`s), then commit all changes to Git. This is to minimize interference with other sub-agents working on files that depend on this one. Sub-agents should have an effort limit - if a lemma proves to be hard, the sub-agent should yield and you should analyze its report to split tasks further or restructure the proof. You must provide enough context for sub-agents: give them detailed, mathematically rigorous proofs for reference, and tell them to make use of Lean skills whenever available.

In all steps, think like a mathematician and explore different proof strategies to minimize formalization effort (you are allowed to spawn sub-agents to explore different proof directions), make use of Mathlib search tools to reuse existing results, and eagerly use Web search and other tools to find useful mathematical materials. You and sub-agents can download important references to the project's `Resources/` folder, and write Markdown summaries alongside with them. You can find in `Resources/` all existing references and summaries.

In any case, you should never give up (e.g. if Mathlib does not contain a prerequisite and cannot be sidestepped, search the Web and think, identify one solution with the least formalization obstacle, and formalize the prerequisites as part of this project). You can increase preset time limits of the Lean compiler (e.g. `maxHeartbeats`).
